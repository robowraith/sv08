# Copilot Instructions

This is the configuration for a Sovol SV08 printer with Mainline Klipper, Demon Macros (Demon Unified Essentials) and Eddy-NG installed.

The printer's shell is reachable via SSH with the command `ssh privat_sv08`. All shell commands should be run from there.
